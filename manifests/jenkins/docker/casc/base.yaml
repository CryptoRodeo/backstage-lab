
jenkins:
  systemMessage: "Backstage Lab Jenkins (ArgoCD-managed) 🚀"
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "admin"
          password: "admin"

unclassified:
  location:
    url: "http://backstage.lab:8080" # edit /etc/hosts , map server IP to hostname (e.g. backstage.lab)

appearance:
  themeManager:
    disableUserThemes: true
    theme: "dark"
